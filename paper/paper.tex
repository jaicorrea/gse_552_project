\documentclass[12pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{setspace}
\usepackage{float}
\onehalfspacing

\title{Replicating Figure~3 from\\
       Akhtari, Bau, and Lalibért{\'e} (2020)}
\author{Jai Correa}
\date{\today}

\begin{document}
\maketitle

% -----------------------------------------------------------------------
\section{The Original Paper}
% -----------------------------------------------------------------------

Akhtari, Mitra, Natalie Bau, and Jean-William P.\ Lalibért{\'e}. 2020.
``Affirmative Action and Pre-College Human Capital.''
\textit{NBER Working Paper} No.\ 27779.
\url{http://www.nber.org/papers/w27779}

The central research question is where affirmative action (AA) affect
students' pre-college human capital investment -- specifically SAT scores
in this replication -- not just outcomes after college admission.
In particular, the paper studied the effect of the 2003 Supreme Court
ruling in \textit{Grutter v.\ Bollinger}, which reinstated race-conscious
admissions at public universities in Texas, Louisiana, and Mississippi
after not doing so for seven years following \textit{Hopwood v.\ Texas}
in 1996.

The paper's primary identification strategy is a difference-in-differences
(DiD) design (Table~1), which compares changes in treated vs.\ control
states before and after 2003. Because event-study plots reveal a
pre-existing downward trend in the treated states' SAT scores relative to
the rest of the country, the authors also employ a synthetic control method
(Figure~3) as a robustness check that explicitly accounts for pre-treatment
divergence.

% -----------------------------------------------------------------------
\section{The Result of Interest}
% -----------------------------------------------------------------------

I replicate \textbf{Figure~3}, the paper's synthetic control estimates of
the effect of AA reinstatement on average math SAT scores for whites and
underrepresented minorities (URMs: Black and Hispanic students).

\medskip
\noindent\textbf{Table~1 context.}
Table~1 in the paper presents the baseline DiD estimates.
The DiD coefficient for URMs' math scores is $8.009$ points (SE $= 1.544$,
$p < 0.01$), and for white students $4.048$ points (SE $= 0.984$,
$p < 0.01$), after absorbing state, year, and race fixed effects.
These estimates are obtained from regressions with 1,904 and 663
observations respectively, with $R^{2}$ of $0.844$ and $0.969$.
Neither verbal scores nor test-taking rates are statistically significant,
suggesting the effect is concentrated in math preparation.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.65\textwidth]{../output/figures/table1.png}
  \caption{Table~1 from Akhtari, Bau, and Lalibért{\'e} (2020): DiD
           estimates of the effect of AA reinstatement on SAT scores
           for URMs (Panel~A) and whites (Panel~B). Standard errors
           in parentheses; *** $p < 0.01$.}
  \label{tab:table1}
\end{figure}

\medskip
\noindent\textbf{Why Figure~3 is needed.}
The DiD estimator rests on the \emph{parallel trends} assumption: in the
absence of treatment, treated and control states would have evolved
identically. Event-study graphs in the paper cast doubt on this assumption:
treated states were already on a declining trajectory relative to controls
in the years immediately before \textit{Grutter}. If that pre-trend
persists into the post-period, the DiD estimate conflates the policy effect
with a pre-existing divergence. Synthetic control addresses this directly by
constructing a counterfactual that matches the treated unit's pre-treatment
trajectory, not merely its pre-treatment mean. Figure~3 is therefore a
robustness check that strengthens confidence in the sign and rough
magnitude of the DiD results in Table~1.

% -----------------------------------------------------------------------
\section{Data and Methods}
% -----------------------------------------------------------------------

\subsection*{Data}

The analysis uses a panel of mean math and verbal SAT scores at the
state--race--year level, digitized from College Board public reports
(1998--2010). The replication package is available at
\url{https://www.openicpsr.org/openicpsr/project/146381/version/V1/view}.
Texas, Louisiana, and Mississippi are collapsed into a single treated
unit (``Texas'') because all three reinstated AA following
\textit{Grutter}. Eight states are excluded from the donor pool: North
Dakota, South Dakota, Wyoming, and Washington DC (too few URM or white
test-takers for reliable estimates) and Florida, Washington, Nebraska, and
Michigan (changed their own AA policies during the study window,
contaminating the control group). The final donor pool contains 40 states.

\subsection*{Synthetic Control: Methodology}

To understand synthetic control, it helps to first write DiD as a
minimization problem \cite{synthdid_notebook}.
The DiD estimator solves:
\[
  \hat{\tau}^{\,did} \;=\; \underset{\mu,\,\alpha,\,\delta,\,\tau}{\arg\min}
    \sum_{i=1}^{N}\sum_{t=1}^{T}
    \Bigl(Y_{it} - \bigl(\beta_0 + \alpha_i + \delta_t
      + \tau\,Treated_{it}\bigr)\Bigr)^2,
\]
where $\alpha_i$ and $\delta_t$ are unit and time fixed effects.
Every observation receives equal weight; identification relies entirely on
the parallel-trends assumption.

Synthetic control modifies this by assigning a unit-specific weight
$\hat{w}^{sc}_i$ to each observation \cite{synthdid_notebook}:
\[
  \hat{\tau}^{\,sc} \;=\; \underset{\delta,\,\tau}{\arg\min}
    \sum_{i=1}^{N}\sum_{t=1}^{T}
    \Bigl(Y_{it} - \delta_t - \tau\,Treated_{it}\Bigr)^2\,\hat{w}^{sc}_i,
    \qquad \text{s.t.}\;\sum_i w_i = 1,\; w_i \geq 0.
\]
The weights $\hat{w}^{sc}_i$ are chosen so that the weighted average of
donor-state outcomes closely tracks the treated unit's pre-treatment
trajectory, minimizing the pre-treatment mean squared prediction error.
Because only donor states enter the weighted average, the counterfactual
is a \emph{data-driven} match rather than the parallel-trends assumption
imposed by DiD.

The estimated treatment effect in each post-period year is the gap between
the treated unit and its synthetic counterfactual:
\[
  \hat{\alpha}_{0t} \;=\; Y_{0t} - \hat{Y}_{0t}^{SC}, \quad t > T_0,
\]
where $\hat{Y}_{0t}^{SC} = \sum_{j} \hat{w}^{sc}_j\,Y_{jt}$ and
$T_0 = 2003$ is the last pre-treatment year.
By construction the pre-treatment gaps are near zero (good fit), so any
systematic post-treatment divergence is attributed to the policy.

A summary DD-style coefficient is computed as:
\[
  \widehat{DD}^{SC} \;=\;
    \frac{1}{|T_{\text{post}}|}\sum_{t \geq T_0}\hat{\alpha}_{0t}
  \;-\;
    \frac{1}{|T_{\text{pre}}|}\sum_{t < T_0}\hat{\alpha}_{0t},
\]
which mirrors the DiD regression coefficient while letting the
pre-treatment trajectory—not just the pre-treatment mean—define the
counterfactual.

\subsection*{Synthetic Control: Application}

The preferred specification (\texttt{spec10}) uses Stata's \texttt{synth}
command with nested (two-level, ``joint'') optimization. The predictor set
includes pre-treatment averages of math and verbal SAT scores for both URM
and non-URM students over two sub-periods (1998--2000 and 2001--2003),
test-take rates, and education spending variables.

The preprocessing script (\texttt{preprocess.py}) loads the working file
and the weight files (\texttt{nonURMm10.dta}, \texttt{URMm10.dta}).
The analysis script (\texttt{analysis.py}) applies these weights year by
year to construct the counterfactual series, computes the gap and DD
coefficient for each group, and produces Figure~\ref{fig:figure3}.

The non-zero donor weights for whites (\texttt{spec10}) are: California
(0.321), Indiana (0.225), Arizona (0.158), West Virginia (0.107), North
Carolina (0.094), Ohio (0.071), Alabama (0.022), and Pennsylvania (0.003).
For URMs: New Jersey (0.248), Pennsylvania (0.232), Nevada (0.222), Ohio
(0.192), Kentucky (0.035), West Virginia (0.042), and Minnesota (0.030).

% -----------------------------------------------------------------------
\section{Replication Results}
% -----------------------------------------------------------------------

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{../output/figures/figure3.png}
  \caption{Replication of Figure~3: Synthetic Control Estimates of the
           Effect of AA on SAT Math Scores.
           Left panel: whites. Right panel: URMs (Black and Hispanic).
           Treated unit = Texas (TX\,+\,LA\,+\,MS combined).
           Vertical red line marks 2003.5 (treatment).
           DD coef labels show mean post-gap minus mean pre-gap.}
  \label{fig:figure3}
\end{figure}

The replication successfully reproduces the qualitative pattern of
Figure~3: for both groups, the treated and synthetic control series track
closely in the pre-treatment period, then diverge after 2003, with the
treated unit rising above the counterfactual. This pattern mirrors the
Table~1 DiD estimates and reinforces the interpretation that AA
reinstatement raised math SAT preparation among both URMs and white
students in treated states.

The implied synthetic control DD estimate for URMs is
$\input{../output/tables/sc_dd_urm.tex}$ points (paper: 10.778) and for
whites is $\input{../output/tables/sc_dd_non.tex}$ points (paper: 6.273).
These are smaller than the paper's reported values by approximately
$\input{../output/tables/sc_diff_urm.tex}$ and
$\input{../output/tables/sc_diff_non.tex}$ points respectively.
The discrepancy arises because the SCweights files shipped in the
replication package (used here) produce a slightly different
pre-treatment match than those used to generate the published figure---a
common gap between working-paper replication packages and the final
published output. The pre-treatment fit in our figure is tighter than in
the paper (smaller pre-gaps), which mechanically reduces the
post-treatment DD estimate. The direction, statistical significance, and
policy interpretation of the result are unchanged: reinstating AA
increased URMs' average math SAT scores by roughly
$\input{../output/tables/sc_dd_urm.tex}$ points and whites' by roughly
$\input{../output/tables/sc_dd_non.tex}$ points---consistent with the DiD
estimates of $8.009$ and $4.048$ points in Table~1.

% -----------------------------------------------------------------------
\section{What I Learned}
% -----------------------------------------------------------------------

The easiest part of the replication was applying the pre-computed weights:
once the working file was merged with the SC weight files, producing the
counterfactual is a single weighted sum per year. The figure closely
matches the visual pattern in the paper even where the exact DD coefficient
differs, and both qualitatively confirm Table~1's DiD finding.

The hardest part was diagnosing \emph{why} our DD estimates differ from
the paper's. By reading both the \texttt{\_Y\_treated} and
\texttt{\_Y\_synthetic} columns embedded in the Stata \texttt{synth}
weight output files, I confirmed that our calculation exactly reproduces
the synthetic estimates stored by Stata---meaning the discrepancy is not
a Python vs.\ Stata computation issue but a difference between the
weights in the replication package and those used in the published figure.
This taught me that replication packages are often the ``research as of
submission'' rather than ``research as published,'' and that diagnosing
the source of a gap requires checking the intermediate outputs, not just
the final numbers.

More broadly, working through both the DiD and synthetic control
specifications deepened my understanding of how researchers use
complementary methods to guard against assumption violations. The parallel
trends assumption underpinning Table~1 is strong; the synthetic control
in Figure~3 relaxes it while producing qualitatively identical results,
which is the strongest kind of robustness check available in
quasi-experimental research.

% -----------------------------------------------------------------------
\begin{thebibliography}{9}
\bibitem{akhtari2020}
  Akhtari, Mitra, Natalie Bau, and Jean-William P.\ Lalibért{\'e}. 2020.
  ``Affirmative Action and Pre-College Human Capital.''
  \textit{NBER Working Paper} No.\ 27779.
\bibitem{synthdid_notebook}
  Vasilaky, Katya. 2024.
  ``Synthetic DiD'' [Course Notebook].
  California Polytechnic State University.
  \texttt{SynthDiD.ipynb}.
\end{thebibliography}

\end{document}
